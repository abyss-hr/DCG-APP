// Placeholder: currently you have LocationContext doing the work.
// Keep this just to avoid TS errors if imported accidentally.
export function useLocations() {
  return {
    locations: [] as string[],
  };
}
